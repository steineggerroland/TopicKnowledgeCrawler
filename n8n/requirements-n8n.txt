# Gleiche Basis wie die Step-Pipeline; im n8n-Container per pip install -r installieren
# (oder Repo als Volume mounten und PYTHONPATH auf das Projektroot setzen).
beautifulsoup4==4.14.3
feedparser==6.0.12
requests==2.33.1
tldextract==5.3.1
trafilatura==2.0.0
