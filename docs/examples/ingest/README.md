# Ingest Examples

These JSON files are contract examples for `POST /api/crawler/ingest`.

- `article.json`: RSS/HTML article payload.
- `episode.json`: Podcast episode payload with media metadata, shownotes and
  parsed chapter markers.

infl0 can use these examples as API fixtures and UI planning references.
