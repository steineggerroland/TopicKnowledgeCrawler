# Source Status Examples

These JSON files are contract examples for `POST /api/crawler/source-status`.
Each file represents one possible `sourceHealthStatus` value sent by
TopicKnowledgeCrawler to infl0.

The examples are intentionally concrete and can be used by infl0 tests, API
fixtures, feed source status views and operator UI prototypes.
